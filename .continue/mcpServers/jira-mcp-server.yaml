name: "Jira MCP Server"
version: "0.0.1"
schema: "v1"
mcpServers:
  - name: "jira"
    command: "npx"
    args:
      - "-y"
      - "@atlassian/mcp-remote"
      - "https://mcp.atlassian.com/v1/mcp"
