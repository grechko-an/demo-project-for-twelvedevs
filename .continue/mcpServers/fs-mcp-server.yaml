name: "Filesystem MCP Server"
version: "0.0.1"
schema: "v1"
mcpServers:
  - name: "local-files"
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "."
