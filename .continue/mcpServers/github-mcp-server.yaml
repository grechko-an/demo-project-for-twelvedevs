name: "GitHub MCP Server"
version: "0.0.1"
schema: "v1"
mcpServers:
  - name: "github"
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-github"
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "github_token"
